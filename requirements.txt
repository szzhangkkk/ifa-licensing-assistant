# ============================================================
# IFA 上牌小助手 (Lili Bot) - Python 依赖
# python 3.12 必需
# ============================================================

# Web 框架
flask>=3.0,<4.0

# HTTP 客户端
requests>=2.28,<3.0

# 向量数据库 + RAG (Milvus Lite 嵌入式)
pymilvus>=3.0,<4.0
milvus-lite>=2.4
sentence-transformers>=3.0,<6.0
numpy>=1.26,<2.0

# 生产服务器（可选，替代 flask dev server）
gunicorn>=22.0

# 降级备选（如果 Milvus 不可用，回退到 ChromaDB）
chromadb>=1.5,<2.0
